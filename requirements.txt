# The one list of what ships. deploy.sh pip-installs this on the Hetzner box,
# and deploy/phala/pyproject.toml -- which uv2nix builds the measured enclave
# image from -- is generated from it by `python -m deploy.render_pyproject`. Add
# a pin here, re-render, then `cd deploy/phala && uv lock`. A commented-out line
# is an uninstalled dependency in both places.
#
# Tools that only check the code (pytest, pip-audit, uv) go in
# requirements-dev.txt, which is installed on neither. What is scanned for
# advisories is the lock rather than this file, because this file names direct
# dependencies only: see deploy/audit.py.

openai==2.53.0
python-dotenv==1.2.2
requests==2.34.2
langsmith==0.10.16
certifi==2026.7.22
standardwebhooks==1.1.0

# Gmail and Calendar, replacing the Node bridge. google-api-python-client makes
# the calls; google-auth holds credentials that carry no refresh token and ask
# backend.custody.tokens for one through its refresh_handler.
google-api-python-client==2.198.0
google-auth==2.56.3

# --- split custody (cosigner/, backend/custody/) ------------------------------
# cryptography: AES-GCM and HKDF on both sides of the nested wrapping.
# requests-oauth2client: DPoP proof construction. Neither Google auth library
# implements DPoP, in Python or in JS, so this is bring-your-own in either
# language. The co-signer signs the proofs; the enclave only ever handles the
# finished header value.
cryptography==50.0.0
requests-oauth2client==1.8.0
# dcap-qvl: Phala's own pure-Rust TDX quote verifier, used by cosigner/attest.py
# to check the enclave's RA-TLS certificate against this box's allowlist. Only
# reached when attestation is `required`; the module is imported on use, so a
# box without it still starts and still fails closed.
dcap-qvl==0.6.1

# --- PII analyzer (optional) -------------------------------------------------
# Off by default. Presidio imports spaCy behind it and costs ~470 MB resident
# before a model is loaded; en_core_web_lg is ~1.1 GB more at first use, per
# process that masks. That does not fit a 2 GB confidential VM, and the box is
# priced by the GB.
#
# Without these, pseudonymizer runs its regex-only path: the owner's own name,
# email and phone, known contacts, API keys, JWTs, private key blocks, email
# addresses and phone numbers are still masked. What is lost is NER, meaning
# names of people not already in the account's contacts. Measured recall on the
# public corpus is 96% with the analyzer and 74% without.
#
# Uncomment all three lines to switch it on. pseudonymizer.analyzer_available()
# picks the presence up on the next start, and the Settings checkbox stops
# being greyed out. Nothing else has to change on the Hetzner box.
#presidio-analyzer==2.2.363
#presidio-anonymizer==2.2.363
#spacy==3.8.13
# The model is not on PyPI; install it with the pinned wheel URL:
#   python -m spacy download en_core_web_lg
# or: pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
#
# Putting it in the enclave image needs the model as a dependency too, which is
# a URL rather than a pin and so cannot be rendered from this file. Add it to
# deploy/phala/pyproject.toml by hand, outside the generated array:
#   [tool.uv.sources]
#   en-core-web-lg = { url = "<the wheel URL above>" }
# and add "en-core-web-lg" to the dependency array after re-rendering. Weigh it
# against the measurement: the spaCy tree was ~23 of the image's packages.
