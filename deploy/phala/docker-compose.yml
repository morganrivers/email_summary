# Five containers, one image each, because one container was one uid.
#
# This file used to run a single service whose entrypoint started the mail
# daemon, the Pub/Sub receiver and the web server as children of one shell. That
# meant one uid (root, since nothing set `user:`) and one process environment
# holding every secret, so a parsing bug in the web server was every mailbox in
# the enclave -- exactly what `deploy/hetzner/*.service.d/` stopped being true on
# the box in commit 01ba733. TDX defends against the host operator; it does
# nothing about a bug in our own code, and the two are different adversaries.
#
# What makes this stronger here than on the box: dstack embeds this file in
# app-compose.json, whose hash is extended as the `compose-hash` launch event
# and measured into RTMR3. So which process runs as which account, and which
# secrets each one is handed, is *attested* rather than merely configured. An
# operator who moves SESSION_SECRET into the mail container changes the
# measurement, and `cosigner/attest.py` stops accepting the client certificate.
# The file modes on the Hetzner box are checkable by nobody but the box.
#
# How the secrets actually arrive, since it decides everything below. dstack
# decrypts `.encrypted-env` to `/dstack/.host-shared/.decrypted-env` on the
# guest filesystem, keeping only names listed in `allowed_envs`;
# `app-compose.service` reads it with `EnvironmentFile=` and `app-compose.sh`
# runs `docker compose up` with no `--env-file` and no passthrough flag. So the
# full set lives in the *compose process* environment, outside every container,
# and a container receives exactly what its own `environment:` block names.
# Interpolation is the partition. Two rules follow:
#
#   * Never bind-mount /dstack/.host-shared into a container. `.decrypted-env`
#     is written with a bare fs::write and no mode, and it is the whole set.
#   * Every name interpolated below must also appear in `allowed_envs` in
#     app-compose.json, or it decrypts to nothing and the service starts
#     unconfigured. That list is set at deploy time by the phala CLI and is not
#     in this repository.
#
# Where the machine may connect to, which the box answers with
# `IPAddressDeny=any` in `deploy/hetzner/hardening.conf` and this file answered
# with nothing at all. The `egress` service is the same CONNECT proxy the box
# runs (`backend/daemons/egress_proxy.py`), the destinations it allows are the
# same `backend/egress.py`, and every role's HTTP clients are pointed at it
# below. Two networks decide who can go around it, and two roles that hold
# nothing are what let every role that holds something stay on the closed one:
#
#   * `inner` is `internal: true`: docker installs no route off the host for it,
#     so a container attached to it alone reaches the internet through `egress`
#     or not at all, and is reached from outside through `ingress` or not at
#     all. `mail`, `web` and `hook` are all on this and nothing else -- every
#     container holding a token, a session secret, an inference key or an
#     account's data.
#   * `edge` is an ordinary bridge, and exactly two containers are on it:
#     `egress`, whose job is leaving, and `ingress`, whose job is being reached.
#     Neither holds a volume, a secret or a guest-agent socket.
#
# `ingress` is what made that possible and is worth stating plainly, because
# this file said the opposite until it existed. A published port on an
# internal-only network never receives forwarded ingress, so `web` and `hook`
# had to be on `edge` to be reachable -- and being on `edge` is a route off the
# host, so for those two the allowlist was a pair of environment variables their
# own HTTP clients chose to honour. An attacker with code execution does not.
# The two roles the control failed to cover were the two facing the internet.
# Now the published ports belong to a TCP forwarder that parses nothing and
# holds nothing, and it forwards inward over `inner`.
#
# NOTE: none of this has been run in a CVM. It is derived from dstack's source
# (dstack-util/src/system_setup.rs, os/common/rootfs/app-compose.{service,sh}),
# and the boot of each role should be watched the first time it deploys.

x-common: &common
  # No shared image: each role runs its own, carrying only its own reachable
  # code (see flake.nix and deploy/phala/image_files.nix). The per-service
  # `image:` lines below are what a role runs, and each is measured into the
  # compose-hash, so the receiver's image cannot silently gain the mail role's
  # custody stack.
  restart: always
  # The sandbox every role gets, and the half of process containment that lives
  # here. The other half is `execguard.py`, a seccomp filter each service
  # installs in its own process, because the thing that actually denies `execve`
  # is seccomp and this deploy path cannot carry a profile: dstack ships and
  # measures `app-compose.json` alone, and `security_opt: seccomp=<path>` is
  # resolved as a file by the compose client, so a profile referenced here is a
  # file that does not exist in the guest. Inline JSON in that field is read as
  # a path too -- checked against docker 29.1.3 rather than assumed. So the
  # filter ships inside the measured image, and these lines cover the processes
  # it is not installed in: the entrypoint shell, and supercronic and its
  # children in `mail`.
  #
  # What each line is for, since a list of hardening flags is otherwise a list
  # of things nobody can remove safely:
  #
  #   * `read_only` makes the image's own filesystem non-writable, so a payload
  #     cannot land beside the code it wants to be mistaken for. /app is already
  #     root-owned and these processes are not root; this is the same statement
  #     for the rest of the rootfs, /nix/store included.
  #   * `cap_drop: ALL` is belt to the numeric `user:` braces. A non-root uid
  #     has no ambient capability anyway, and stating it means a future
  #     `user: "0:0"` edit does not quietly grant fifteen of them.
  #   * `no-new-privileges` stops a setuid binary from raising privilege across
  #     an exec. It is also what `execguard.py` sets with `prctl` for its own
  #     process, and there is no harm in the kernel hearing it twice.
  #   * `pids_limit` is per role below. It bounds a fork bomb and a miner; it is
  #     deliberately not the exec control, because `os.execv` in place creates
  #     no new task and would pass any ceiling.
  #
  # `tmpfs` is per role rather than here: `mail` and `web` also mount
  # /app/attestation and a service's own `tmpfs:` key replaces this one whole
  # rather than adding to it. Every role's list is checked in
  # tests/test_enclave_boundary.py.
  read_only: true
  cap_drop:
    - ALL
  security_opt:
    - "no-new-privileges:true"
  environment: &common-env
    # Fail-closed attestation gate; see backend/tee/tee_boot.py. Also what makes
    # backend/secrets.py read no file at all, which is why no service below
    # mounts one.
    TEE_REQUIRED: "1"
    # Which compose this image is supposed to be running under. Empty is
    # refused: `tee_boot._measurement_gap` used to return silently on an unset
    # value, so the check was on only for whoever remembered to set it. It must
    # be interpolated rather than written here -- the value is the hash of this
    # file, which cannot contain it.
    EXPECTED_COMPOSE_HASH: "${EXPECTED_COMPOSE_HASH:-}"
    # Both cases of every variable, because the HTTP clients in this tree
    # disagree about which they read; the same six lines are in
    # deploy/hetzner/hardening.conf and for the same reason. `egress` is the
    # service name, resolved by docker's embedded DNS on both networks below.
    HTTP_PROXY: "http://egress:8792"
    HTTPS_PROXY: "http://egress:8792"
    http_proxy: "http://egress:8792"
    https_proxy: "http://egress:8792"
    NO_PROXY: "127.0.0.1,::1,localhost"
    no_proxy: "127.0.0.1,::1,localhost"

services:
  # The only role that opens a mailbox. Holds the Google client credentials, the
  # inference keys and the Telegram token, and is the only account that can read
  # database/<id>/token.bin (0600 to this uid, set by backend/paths.py).
  # The only role holding the Polar API token: it applies spooled Polar events,
  # runs the reconcile, and answers the web role's three billing operations over
  # handoff. That token is organization-wide, which is why it lives with the
  # role that writes plan_status rather than with the one parsing HTTP.
  mail:
    <<: *common
    # Each `image:` line is rewritten to a pinned registry digest by
    #   REGISTRY=<repo> deploy/phala/build_and_publish.sh --push
    # It must carry a literal @sha256 digest in production (not a tag): the
    # compose file is measured into the dstack attestation, so a mutable tag or
    # a ${VAR} would let the operator swap the image and break the trust story.
    # The bare tag here is only a pre-publish placeholder.
    image: tee-email-bot-mail:latest
    command: ["mail"]
    user: "10001:10001"
    # The enclave's SupplementaryGroups=, and it has to be spelled out: a
    # numeric `user:` makes the runtime skip the /etc/group lookup that a
    # username would trigger, so the accounts baked into the image would carry
    # no membership at all and database/ at 2770 would be unreadable. 10010 is
    # letterlock-data, 10011 letterlock-wake -- see the `accounts` set in
    # flake.nix, which is where both numbers and the names come from.
    group_add: ["10010", "10011"]
    environment:
      <<: *common-env
      DEEPSEEK_API_KEY: "${DEEPSEEK_API_KEY:-}"
      NEARAI_API_KEY: "${NEARAI_API_KEY:-}"
      TELEGRAM_BOT_TOKEN: "${TELEGRAM_BOT_TOKEN:-}"
      TELEGRAM_CHAT_ID: "${TELEGRAM_CHAT_ID:-}"
      GOOGLE_OAUTH_CLIENT_ID: "${GOOGLE_OAUTH_CLIENT_ID:-}"
      GOOGLE_OAUTH_CLIENT_SECRET: "${GOOGLE_OAUTH_CLIENT_SECRET:-}"
      # No POLAR_WEBHOOK_SECRET, here or anywhere: no role is a Polar receiver.
      # Entitlement in the enclave is exactly two things, `confirm_checkout()`
      # in `web` and the 3-hourly reconcile in this role's crontab, and both
      # read Polar's API rather than verify an event signature. There is no
      # correct value to inject, which is why `secrets.ROLE_EXEMPT` names that
      # check rather than the boot gate demanding it of somebody.
      POLAR_API_TOKEN: "${POLAR_API_TOKEN:-}"
      POLAR_ORGANIZATION_ID: "${POLAR_ORGANIZATION_ID:-}"
      POLAR_PRODUCT_ID: "${POLAR_PRODUCT_ID:-}"
      POLAR_CHECKOUT_URL: "${POLAR_CHECKOUT_URL:-}"
      POLAR_SANDBOX: "${POLAR_SANDBOX:-0}"
      # This deployment's own hostnames. backend/site.py compiles in the
      # Hetzner ones, and every externally visible URL is built from them: the
      # OAuth redirect, the checkout return, every link in a Telegram message.
      # A CVM that left them at the default would send its users to the other
      # box, which signs cookies with a different key -- so the boot gate
      # refuses to start a role whose hosts are still the compiled-in defaults.
      LETTERLOCK_HOST: "${LETTERLOCK_HOST}"
      LETTERLOCK_API_HOST: "${LETTERLOCK_API_HOST}"
      # The co-signer holding the outer half of every token's custody. Empty
      # falls back to the default host in backend/site.py. This enclave
      # processes no mail at all while that box is unreachable, by design: see
      # docs/plan_token_custody.md §1 on the availability trade.
      LETTERLOCK_COSIGNER_URL: "${LETTERLOCK_COSIGNER_URL:-}"
    # The only role on the internal network alone, and the only one for which
    # the allowlist below is enforced rather than configured: it publishes no
    # port, so nothing has to reach it from outside and docker can refuse it a
    # route out. Everything it does reach -- Google, the inference provider,
    # Telegram, Polar, the co-signer -- goes through `egress`.
    networks:
      - inner
    volumes:
      - database:/app/database
      - state:/app/state
      # dstack guest-agent socket: in-enclave KMS unseal + on-demand attestation.
      - /var/run/dstack.sock:/var/run/dstack.sock
    # RA-TLS material and the boot attestation record written by tee_boot, on a
    # tmpfs so the enclave-held private key never lands on the encrypted volume
    # at rest. Per container, not shared: each role derives its own client
    # certificate from GetTlsKey, so no private key crosses a container
    # boundary.
    #
    # 0700 owned by this role's uid, and not the 0777 it used to be. The long
    # `volumes:` syntax takes only `size` and `mode`, so the directory arrived
    # owned by root and had to be world-writable for a non-root process to use
    # it at all; the excuse written here was that nothing else runs in the
    # container to read it. That is the assumption an intrusion breaks, and it
    # is the one thing this file must never rest on. The service-level `tmpfs:`
    # key takes `uid`/`gid` as tmpfs mount options, which is the whole fix --
    # checked against docker 29.1.3, not assumed.
    #
    # `noexec` on both entries because these are the only writable paths in the
    # container that are not a volume, and a writable path a payload can be
    # executed from is the path it will be written to. /tmp is here for the same
    # reason and because `read_only` above leaves the process nowhere else.
    tmpfs:
      - "/tmp:mode=1777,noexec,nosuid,nodev,size=64m"
      - "/app/attestation:mode=0700,uid=10001,gid=10001,noexec,nosuid,nodev,size=4m"
    # Room for the daemon, its handoff listener, its request threads, the
    # watcher, supercronic and whatever the crontab is running. Bounds a fork
    # bomb; see the anchor for why it is not the exec control.
    pids_limit: 256

  # The product UI. Holds a data key for rendering an account's own documents
  # and so runs the attestation gate, but never a Google token: token.bin is
  # 0600 to the mail uid, and the consent URL, the OAuth callback and voice
  # generation cross to the mail role over backend/custody/handoff.py.
  #
  # It carries no inference key. Which providers Settings offers is decided by
  # whether their keys are present, and this process used to hold two live keys
  # for no other purpose than answering that. It now asks the mail role over
  # handoff.OP_PROVIDERS, which sends names and never a key. The cost is that
  # /settings returns 503 while the mail role is down rather than rendering as
  # though one provider were configured.
  #
  # It carries no Polar token either, for the same reason and a larger one.
  # POLAR_API_TOKEN is organization-wide: it reads every customer in the org
  # and mints a portal session for any customer id, so a parsing bug in the
  # process facing the open internet reached the whole billing org rather than
  # this deployment's own accounts. The three calls it needed -- mint a
  # checkout, read one back on the return page, mint a portal session -- cross
  # to the mail role over handoff, which is also the role that writes
  # plan_status. What remains reachable from here is what the handoff will do
  # when asked: mint a checkout for a named account, and mint a portal link for
  # an account that already has a linked Polar customer.
  web:
    <<: *common
    image: tee-email-bot-web:latest
    command: ["web"]
    user: "10002:10002"
    # letterlock-data only. Not letterlock-wake: writing the wake spool starts a
    # drafting pass against an account of the writer's choosing and spends that
    # account's co-signer budget, which is the same reason the web uid is kept
    # out of it on the box.
    group_add: ["10010"]
    environment:
      <<: *common-env
      SESSION_SECRET: "${SESSION_SECRET:-}"
      SESSION_SECRET_PREVIOUS: "${SESSION_SECRET_PREVIOUS:-}"
      # See the mail role: every link this role renders, and the return page a
      # buyer lands back on, must name this deployment rather than the box.
      LETTERLOCK_HOST: "${LETTERLOCK_HOST}"
      LETTERLOCK_API_HOST: "${LETTERLOCK_API_HOST}"
      LETTERLOCK_COSIGNER_URL: "${LETTERLOCK_COSIGNER_URL:-}"
      # The one interface the product is served on. It binds loopback by
      # default (`site.LOOPBACK`), which on the box is right -- Caddy is in
      # front of it there -- and in here meant the published port below reached
      # a socket listening on the container's own loopback and the UI answered
      # nobody.
      WEB_HOST: "0.0.0.0"
      # Which peer's X-Forwarded-For `web_server._source_ip` may believe.
      # Loopback is always trusted; this names whatever dstack's ingress
      # connects from, and until somebody has watched a real CVM and can say
      # what that address is, it stays empty. Empty costs a wrong audit row
      # (every sign-in recorded as coming from the ingress); a guess costs a
      # forgeable one, which is worse. Naming it here also means adding the
      # variable to `allowed_envs` in app-compose.json.
      LETTERLOCK_TRUSTED_PROXIES: "${WEB_TRUSTED_PROXIES:-}"
    # `inner` alone, and no published port. This role used to be on `edge` too,
    # because a published port on an internal-only network never receives
    # forwarded ingress and the product's UI has to be reachable -- and being on
    # `edge` gave it a route off the host, so the egress allowlist was two
    # environment variables its own HTTP client chose to honour rather than
    # anything docker enforced. `ingress` holds the published port now and
    # forwards in over this network, so the UI is reachable and the route out is
    # gone. See the header.
    networks:
      - inner
    volumes:
      - database:/app/database
      - state:/app/state
      - /var/run/dstack.sock:/var/run/dstack.sock
    # Owned by this role's uid, not the mail role's: the two containers each get
    # their own tmpfs and each derives its own client certificate, so neither
    # directory should be readable by the other's account even in the case where
    # a future edit shares one.
    tmpfs:
      - "/tmp:mode=1777,noexec,nosuid,nodev,size=64m"
      - "/app/attestation:mode=0700,uid=10002,gid=10002,noexec,nosuid,nodev,size=4m"
    # A thread per request plus the watcher. Matches TasksMax in the box's
    # deploy/hetzner/hardening.conf, which fronts the same server.
    pids_limit: 128

  # Verifies the Pub/Sub OIDC JWT and appends an address to the wake spool. The
  # cheapest role to isolate and the most exposed: it is the process Google
  # posts to, and it needs nothing.
  #
  # No database volume, so it cannot read the account list -- it does not
  # resolve the address it receives, the mail role does. No guest-agent socket,
  # which is why it runs no attestation gate: it holds no key to unseal and no
  # certificate to obtain, and the address it spools is acted on by a role that
  # is gated. Withholding the socket is the whole point, since that socket is
  # unauthenticated and GetKey takes a caller-supplied derivation path, so any
  # process that can open it can derive this app's sealing key.
  hook:
    <<: *common
    image: tee-email-bot-hook:latest
    command: ["hook"]
    user: "10003:10003"
    # letterlock-wake alone: the FIFO and the wake spool, and nothing else in
    # state/. Deliberately not letterlock-data, which is what keeps the account
    # list out of reach of the process Google posts to.
    group_add: ["10011"]
    environment:
      <<: *common-env
      WEBHOOK_HOST: "0.0.0.0"
      WEBHOOK_PORT: "8787"
      WEBHOOK_AUD: "${WEBHOOK_AUD}"
      PUBSUB_SERVICE_ACCOUNT: "${PUBSUB_SERVICE_ACCOUNT}"
    # `inner` alone, and no published port, for the reason `web` has none:
    # `ingress` takes what Google posts and forwards it here. It reaches the
    # network for one thing only, verifying the Pub/Sub token's signature, and
    # that goes through `egress` like everything else -- now because it has no
    # other route rather than because it was asked nicely.
    networks:
      - inner
    volumes:
      - state:/app/state
    # No attestation directory: this role is deliberately given no guest-agent
    # socket, so it runs no gate and derives no certificate. /tmp alone, because
    # `read_only` leaves a process nowhere to put a temporary file and the
    # stdlib will reach for one.
    tmpfs:
      - "/tmp:mode=1777,noexec,nosuid,nodev,size=64m"
    # The process Google posts to, and the one that needs the least: a thread
    # per request and the watcher.
    pids_limit: 64

  # The only container allowed to open a connection to the internet, and the
  # same code the box runs as `egress-proxy.service`. An HTTP CONNECT proxy on
  # 8792: it reads one request line, checks the hostname against
  # `backend/egress.py`, and moves opaque bytes. TLS is end to end between the
  # role and the destination, so this process holds no decrypted byte and no key
  # that would let it.
  #
  # It is the cheapest role to isolate after `hook` and it needs even less: no
  # volume, no guest-agent socket, no secret, and therefore no attestation gate
  # (it has nothing to unseal). What it does need is the one thing no other role
  # has, which is why it is a container of its own rather than a thread in the
  # mail daemon: on the box the process holding the network is deliberately not
  # the process holding the API keys.
  #
  # It publishes no port. The only things that can reach 8792 are containers on
  # these two networks, which is every role and nothing else. It does not
  # authenticate its callers -- the boundary is over destinations, not callers,
  # and `inner` is what stops a role going around it.
  egress:
    <<: *common
    image: tee-email-bot-egress:latest
    command: ["egress"]
    user: "10004:10004"
    environment:
      # Deliberately not *common-env: this container must not be pointed at
      # itself, and it needs none of the rest. TEE_REQUIRED is here so
      # backend/secrets.py reads no file, the same as everywhere else.
      TEE_REQUIRED: "1"
      # Its clients are other containers, so loopback would answer none of them.
      EGRESS_PROXY_BIND: "0.0.0.0"
    networks:
      - inner
      - edge
    # No volume of any kind, so /tmp is the whole of this container's writable
    # filesystem, and it is noexec.
    tmpfs:
      - "/tmp:mode=1777,noexec,nosuid,nodev,size=64m"
    # A thread per tunnel, and every role's outbound traffic comes through here.
    pids_limit: 128

  # The only container the outside world connects to, and what makes the
  # allowlist above enforcement for every role that holds anything.
  #
  # Before it, `web` and `hook` had to be on `edge` -- they must receive traffic
  # from outside the CVM, and a published port on an internal-only network never
  # receives forwarded ingress. Being on `edge` is a route off the host, so for
  # those two the proxy was a pair of environment variables their own HTTP
  # clients honoured, which an attacker with code execution does not. The two
  # roles holding a session secret and an account's data were the two the
  # control did not cover.
  #
  # This takes the published ports and forwards inward over `inner`, so both
  # move to the internal network alone: still reachable, no longer routable.
  # It is a TCP forwarder and parses nothing -- see the module docstring for why
  # that matters and for the one thing it costs, which is that `web` sees this
  # container as its peer rather than a browser.
  #
  # It is the cheapest role in the file: no volume, no guest-agent socket, no
  # secret, no gate, no group, and a routing table fixed before it accepts a
  # connection.
  ingress:
    <<: *common
    image: tee-email-bot-ingress:latest
    command: ["ingress"]
    user: "10005:10005"
    environment:
      # Deliberately not *common-env. It needs no secret and must not be pointed
      # at the egress proxy: it originates nothing, it only answers. TEE_REQUIRED
      # is here so backend/secrets.py reads no file, the same as everywhere else.
      TEE_REQUIRED: "1"
      # The one listener in this tree that has to accept from off the host.
      INGRESS_PROXY_BIND: "0.0.0.0"
    # Both networks, and the only container for which that is the job rather
    # than a compromise: `edge` to be reached, `inner` to reach `web` and `hook`
    # by service name.
    networks:
      - inner
      - edge
    ports:
      - "8790:8790"
      - "8787:8787"
    # The container the open internet connects to, and the one with the least
    # inside it to reach: no volume, and a noexec /tmp.
    tmpfs:
      - "/tmp:mode=1777,noexec,nosuid,nodev,size=64m"
    # A thread per connection, for both published ports.
    pids_limit: 128

volumes:
  database:
  state:

networks:
  # No route off the host. A container attached to this alone reaches the
  # internet through `egress` or not at all, and is reached from outside the CVM
  # through `ingress` or not at all. Every role that holds data is on this and
  # nothing else.
  inner:
    internal: true
  # An ordinary bridge, for the one container that must be reached from outside
  # the CVM and the one that must leave it. Neither holds anything.
  edge:
